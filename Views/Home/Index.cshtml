@model ui_client_dotnetcore.Models.ViewModel

@{    
    ViewData["Title"] = "Home Page";
}

<!-- Web Component -->
<taco-header></taco-header>

<div class="seperator" />

<!-- Flat HTML -->
@Html.Raw(Model.Tacos)     

<div class="seperator" />

<!-- React component -->
<my-component></my-component>

<div class="seperator" />

<!-- Angular component -->
<taco-stats></taco-stats>

<script>
  // Inject JS for vanilla JS web component
  @Html.Raw(Model.Header)

  // Inject JS for React web component
  @Html.Raw(Model.Deals)

  // Inject JS for Angular web component
  @Html.Raw(Model.Stats)
</script>